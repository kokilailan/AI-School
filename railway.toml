[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run deploy"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
NODE_ENV = "production"
NIXPACKS_NODE_VERSION = "22"
NIXPACKS_PATH = "/usr/local/bin"
